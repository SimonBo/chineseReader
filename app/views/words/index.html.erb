<h1>Listing words</h1>

<table>
  <thead>
    <tr>
      <th>Traditional char</th>
      <th>Simplified char</th>
      <th>Pronunciation</th>
      <th>Meaning</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @words.each do |word| %>
      <tr>
        <td><%= word.traditional_char %></td>
        <td><%= word.simplified_char %></td>
        <td><%= word.pronunciation %></td>
        <td><%= word.meaning %></td>
        <td><%= link_to 'Show', word %></td>
        <td><%= link_to 'Edit', edit_word_path(word) %></td>
        <td><%= link_to 'Destroy', word, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Word', new_word_path %>
