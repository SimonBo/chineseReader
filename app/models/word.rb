class Word < ActiveRecord::Base
  # def self.find_words(word_index, text)
  #   possible_words = []
  #   word_index = word_index.to_i
  #   text[word_index..-1].each_with_index do |word, index|
  #     # new_index = index + word_index +1
  #     new_word = text[word_index..
  #     if Word.where('simplified_char = ?', )
  #       possible_words << Word.where('simplified_char = ?', text[word_index.to_i..next_char])
  #     else
  #       break
  #     end
  #   end

  #   return possible_words.last
  # end
end
